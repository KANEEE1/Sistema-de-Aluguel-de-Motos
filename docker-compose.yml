services:
  rental-system-api:
    build:
      context: .
      dockerfile: src/RentalSystem.Api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres-db;Port=5432;Database=rental_system_db;Username=rental_user;Password=rental_password
      - RabbitMQ__Host=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
    depends_on:
      - postgres-db
      - rabbitmq

  postgres-db:
    image: postgres:13
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=rental_system_db
      - POSTGRES_USER=rental_user
      - POSTGRES_PASSWORD=rental_password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672" # AMQP protocol
      - "15672:15672" # Management UI
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest

volumes:
  postgres_data:
